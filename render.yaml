services:
  - type: web
    name: Cars_Sense
    env: python
    buildCommand: |
      apt-get update && apt-get install -y build-essential libblas-dev liblapack-dev
      pip install -r requirements.txt
    startCommand: gunicorn app:app
